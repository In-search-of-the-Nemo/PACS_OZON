# Оркестратор (запуск сервисов по Procfile)
honcho

# Serviz backend (веб-сервер + WebSocket)
Flask
Flask-SocketIO
python-socketio
python-engineio
simple-websocket

# Transnet (декодирование SSL-протокола)
protobuf
cattrs

# Общие
pyyaml
attrs

# ZeroMQ — межпроцессная шина (ether) между serviz и transnet
pyzmq

# Только для перегенерации protobuf (transnet/build_proto.sh); в рантайме не нужен
protoletariat
