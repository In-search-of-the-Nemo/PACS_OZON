# Оркестратор (запуск сервисов по Procfile)
honcho

# Serviz backend (веб-сервер + WebSocket)
Flask
Flask-SocketIO
python-socketio
python-engineio
simple-websocket

# Общие
pyyaml
attrs

# ZeroMQ — межпроцессная шина (ether) между serviz и transnet
pyzmq
